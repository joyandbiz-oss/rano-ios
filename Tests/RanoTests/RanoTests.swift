import XCTest
@testable import Rano

/// Skeleton — fill once V1 logic lands.
/// For Day 0-1 prototype the actual validation is manual (PlaygroundView on physical iPhone).
final class RanoTests: XCTestCase {

    func testPlaceholder() {
        XCTAssertTrue(true)
    }
}
